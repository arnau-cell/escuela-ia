# Copia este archivo a .env.local y rellena el valor real. .env.local nunca se sube a git
# (ver .gitignore). En producción, la clave real vive como secret de Cloudflare Workers:
# `wrangler secret put ANTHROPIC_API_KEY`.

ANTHROPIC_API_KEY=
