# Dependencias y build
node_modules/
.astro/
dist/
.cache/

# Entorno y secretos (NUNCA subir)
.env
.env.*
!.env.example

# Datos / PII (nunca a GitHub)
_DATOS/
*.local

# Estrategia privada (legal, pitch, aceleradoras, protocolo) — nunca a GitHub (repo público)
_privado/

# Scripts efímeros
.dot_*

# OS / editor
.DS_Store
Thumbs.db
