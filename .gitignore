# Dependencias y build
node_modules/
.astro/
dist/
.cache/
.wrangler/

# Generado por scripts/generate-wiki-context.mjs (predev/prebuild) a partir del contenido real
# de wiki-ia/* — no es fuente de verdad, se regenera solo.
src/data/wiki-ia/content.generated.json

# Entorno y secretos (NUNCA subir)
.env
.env.*
!.env.example
.dev.vars

# Datos / PII (nunca a GitHub)
_DATOS/
*.local

# Estrategia privada (legal, pitch, aceleradoras, protocolo) — nunca a GitHub (repo público)
_privado/
# Material de inversores/pitch generado fuera de _privado/ (incidente 2026-07-05: una sesión
# dejó pitch decks en outputs/ sin ignorar — deben vivir en _privado/ o Drive, nunca en el repo)
outputs/
# Registros de sesión pendientes de archivar (contexto interno, no producto)
_registro_pendiente/

# Scripts efímeros
.dot_*

# OS / editor
.DS_Store
Thumbs.db
